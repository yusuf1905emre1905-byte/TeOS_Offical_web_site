<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Free TeOS — Freedom Inside</title>
  <style>
    body {
      margin: 0;
      font-family: "Segoe UI", Arial, sans-serif;
      background: linear-gradient(135deg, #121212, #222);
      color: #f0f0f0;
      overflow: hidden;
    }

    header {
      background: #101010;
      color: #00e0ff;
      text-align: center;
      padding: 20px;
      font-size: 2em;
      font-weight: bold;
      letter-spacing: 2px;
      box-shadow: 0 0 15px #00e0ff66;
    }

    .taskbar {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 40px;
      background: #1a1a1a;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 15px;
      box-shadow: 0 -2px 5px rgba(0,0,0,0.4);
    }

    .taskbar button {
      background: #00e0ff;
      color: #000;
      border: none;
      padding: 8px 15px;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .taskbar button:hover {
      background: #00a4cc;
    }

    .window {
      position: absolute;
      top: 70px;
      left: 50%;
      transform: translateX(-50%);
      background: #181818;
      border: 2px solid #00e0ff;
      border-radius: 10px;
      width: 80%;
      max-width: 900px;
      padding: 20px;
      box-shadow: 0 0 30px #00e0ff55;
      animation: fadeIn 1s ease;
    }

    .window h2 {
      color: #00e0ff;
      margin-top: 0;
    }

    .apps {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }

    .app {
      background: #222;
      border-radius: 10px;
      text-align: center;
      padding: 10px;
      box-shadow: 0 0 10px #00e0ff33;
      cursor: pointer;
      transition: 0.3s;
    }

    .app:hover {
      background: #00e0ff22;
      transform: scale(1.05);
    }

    .footer {
      text-align: center;
      padding: 15px;
      font-size: 0.9em;
      color: #aaa;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <header>🔨 Free TeOS — Freedom Inside</header>

  <div class="window">
    <h2>TeOS Uygulama Merkezi</h2>
    <p>TeOS, geleceğin özgür işletim sistemi. Güç, hız ve özgürlük tek bir yerde birleşiyor!</p>
    <div class="apps">
      <div class="app">🧠 TekonAI</div>
      <div class="app">🛡️ TeSecurity</div>
      <div class="app">🧰 TeDev</div>
      <div class="app">🛒 TeStore</div>
      <div class="app">📺 UserTV</div>
      <div class="app">🐾 My Pet Takky</div>
      <div class="app">🦖 Cubby</div>
      <div class="app">🐶 Doggy</div>
      <div class="app">🕊️ Borty</div>
    </div>
    <br>
    <button style="background:#00e0ff;color:#000;padding:10px 20px;border:none;border-radius:8px;font-weight:bold;cursor:pointer;">
      📦 Şimdi İndir (Free TPK)
    </button>
  </div>

  <div class="taskbar">
    <div>🌐 TeOS Universal 2025</div>
    <button onclick="alert('Başlat menüsü yakında aktif olacak!')">Başlat</button>
  </div>

  <div class="footer">© 2025 TeOS Universal. All Rights Reserved.</div>
</body>
</html>
